apiVersion: v1
kind: Service
metadata:
  name: message-service  # [핵심] metadata.name == 도메인 주소(DNS)
spec:
  selector:
    app: message-service
  ports:
    - port: 8080         # 서비스 포트 (다른 파드가 호출할 때 쓰는 포트)
      targetPort: 8080   # 실제 컨테이너 포트