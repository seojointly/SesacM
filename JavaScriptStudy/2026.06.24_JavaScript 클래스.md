markdown_content = """# JavaScript 클래스에서 Private 프로퍼티 직접 할당 대신 Setter를 거쳐야 하는 이유

제공된 코드의 `constructor`를 살펴보면, `#price`와 `#quantity` 두 private 필드를 초기화하는 방식에 차이가 있습니다.

코드 출력
File generated successfully.

```javascript
constructor(name, quantity = 1, price) {
  this.name = name;
  this.#price = price;       // ❌ private 프로퍼티에 직접 할당
  this.quantity = quantity;  //  setter를 거쳐 #quantity에 전달 (권장 방식)
}
생성자(Constructor) 내부에서 private 프로퍼티(this.#quantity)에 값을 직접 할당하는 것보다, this.quantity = quantity;와 같이 정의된 Setter를 거쳐서 값을 전달하는 것이 더 좋은 설계인 이유는 다음과 같습니다.

1. 데이터 무결성 보장 및 검증(Validation)의 재사용
Setter를 사용하는 가장 큰 목적은 클래스 외부나 내부에서 필드에 부적절한 값이 들어오는 것을 방지하는 것입니다.
만약 생성자에서 private 필드에 직접 값을 할당하면, 생성 시점에는 Setter에 구현된 검증 로직이 작동하지 않습니다.

❌ private 필드 직접 할당 시의 문제점 (#price)
만약 가격(price)이나 수량(quantity)은 음수가 될 수 없다는 비즈니스 로직이 Setter에 추가된다고 가정해 보겠습니다.

JavaScript
set price(value) {
  if (value < 0) {
    throw new Error("가격은 음수일 수 없습니다.");
  }
  this.#price = value;
}
위와 같이 Setter에 검증 로직을 넣었더라도, 생성자에서 this.#price = price;와 같이 직접 할당하면 아래와 같은 치명적인 오류가 발생할 수 있습니다.

JavaScript
// 잘못된 값(-500)을 넣었음에도 에러 없이 인스턴스가 생성됨 (검증 우회)
const item = new CartItem("상품A", 1, -500); 

// 이후에 변경할 때만 에러가 발생함
item.price = -1000; // Error: 가격은 음수일 수 없습니다.
결과적으로 데이터의 무결성이 깨진 비정상적인 객체가 생성될 수 있습니다.

Setter를 거쳐서 할당 시의 이점 (quantity)
반면 수량(quantity)처럼 Setter를 통해 할당하도록 작성하면, 인스턴스를 생성하는 시점에도 Setter 내부의 검증 로직을 그대로 통과해야 하므로 안전합니다.

JavaScript
set quantity(value) {
  if (!Number.isInteger(value) || value < 1) {
    throw new Error("수량은 1개 이상의 정수여야 합니다.");
  }
  this.#quantity = value;
}

// 생성 시점부터 에러를 발생시켜 비정상 객체 생성을 원천 차단
const item2 = new CartItem("상품B", -5, 1000); // 즉시 Error 발생!
2. 코드 중복 제거 (DRY 원칙: Don't Repeat Yourself)
만약 생성자에서 private 프로퍼티에 직접 값을 넣으면서 검증 로직도 적용하고 싶다면, 생성자 안에도 Setter와 동일한 에러 체크 로직을 똑같이 작성해야 합니다.

JavaScript
constructor(name, quantity = 1, price) {
  this.name = name;
  
  // 검증 로직 중복 발생
  if (price < 0) throw new Error("가격은 음수일 수 없습니다.");
  this.#price = price;
  
  this.quantity = quantity; // Setter만 타면 되므로 깔끔함
}
로직이 중복되면 향후 유지보수 시(예: "최소 가격은 100원 이상이어야 한다"로 규칙이 바뀔 때) 생성자와 Setter 두 곳의 코드를 모두 수정해야 하므로 버그가 발생할 확률이 높아집니다.

3. 부수 효과(Side Effects)의 일관된 실행
Setter는 단순 값 저장을 넘어, 값이 변경될 때 다른 상태를 함께 업데이트하는 부수 효과(Side Effect)를 포함하는 경우가 많습니다.
예를 들어, 장바구니 아이템의 수량이 바뀔 때마다 화면을 자동으로 다시 그리거나(렌더링), 총 결제 금액을 실시간으로 다시 계산해야 할 수 있습니다.

JavaScript
set quantity(value) {
  this.#quantity = value;
  this.updateSubtotal(); // 수량 변경 시 소계 금액을 자동 갱신하는 부수 효과
}
생성자에서 this.quantity = quantity;를 호출하면 객체가 처음 만들어질 때도 이 부수 효과가 누락 없이 완벽하게 실행됩니다. 반면 private 필드에 직접 넣으면 초기 생성 시에는 해당 부수 효과가 실행되지 않아 초기 데이터 불일치가 발생할 수 있습니다.

요약
this.#price = price (비권장): 캡슐화된 private 변수에 직접 접근하므로, Setter에 정의된 데이터 검증 및 부수 효과 로직을 우회하게 되어 객체의 안전성이 떨어집니다.

this.quantity = quantity (권장): 인스턴스 생성 시점부터 Setter를 강제로 거치게 만들어, 올바른 데이터만 객체에 담기도록 강제(데이터 무결성 확보)하고 코드를 단일화할 수 있습니다.
"""

with open("CartItem_Setter_Reason.md", "w", encoding="utf-8") as f:
f.write(markdown_content)
print("File generated successfully.")

수량(quantity)을 초기화할 때 private 프로퍼티인 `this.#quantity`에 직접 대입하지 않고, Setter인 `this.quantity = quantity;`를 거쳐서 전달하는 것이 왜 더 좋은 설계인지 그 이유를 상세히 분석한 `.md` 파일을 생성했습니다.

Your MD file is ready
[file-tag: code-generated-file-0-1782265712362320656]

### 핵심 이유 요약
1. **데이터 무결성 및 검증(Validation) 로직 재사용:** Setter에 수량이 음수가 될 수 없거나 정수여야 한다는 검증 로직이 추가되었을 때, 생성자에서 직접 private 필드에 값을 넣으면 초기 생성 시 이 검증을 우회하여 비정상적인 객체가 생성될 수 있습니다.
2. **중복 코드 제거 (DRY 원칙):** 생성자와 Setter에 각각 동일한 데이터 검증 코드를 작성할 필요 없이, Setter 한 곳으로 로직을 일원화하여 유지보수성을 높입니다.
3. **일관된 부수 효과(Side Effects) 실행:** 수량이 바뀔 때 장바구니 총액을 재계산하거나 화면을 갱신하는 등의 추가 로직이 Setter에 구현되어 있다면, 생성 시점에도 이 로직이 누락 없이 똑같이 실행되도록 보장합니다.